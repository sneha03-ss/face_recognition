FACE RECOGNITION 

This project includes a program for encoding reference images along with a main file to execute the face recognition system. It requires creating a folder named “faces” that contains images of individuals to be recognized. The “faces” folder should be placed in the same directory as the program files.
